name: Agent Framework Quality

on:
  push:
    branches: [main]
    paths:
      - 'AGENTS.md'
      - 'README.md'
      - 'agent/**'
      - 'validation/**'
      - '.githooks/**'
      - '.github/workflows/agent-framework-quality.yaml'
  pull_request:
    paths:
      - 'AGENTS.md'
      - 'README.md'
      - 'agent/**'
      - 'validation/**'
      - '.githooks/**'
      - '.github/workflows/agent-framework-quality.yaml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pydantic-contracts:
    name: Pydantic Contracts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.14'

      - name: Install validation dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -r validation/core/requirements.txt

      - name: Validate registered YAML contracts
        run: python3 -m validation.core.validate

  execution-item-invariants:
    name: Execution Item Invariants
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.14'

      - name: Install validation dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -r validation/core/requirements.txt

      - name: Validate execution item state invariants
        run: agent/scripts/check/check-execution-items.sh

  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Install shellcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Run shell script lint
        run: agent/scripts/check/shellcheck.sh

  task-contracts:
    name: Task Contracts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.14'

      - name: Install validation dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -r validation/core/requirements.txt

      - name: Validate task contracts
        run: |
          set -euo pipefail
          for task in \
            discuss-plan \
            refine-plan \
            plan-item \
            implement-item \
            review-item \
            finalize-plan \
            review-architecture \
            review-consistency \
            review-quality \
            review-security \
            review-technology \
            freestyle \
            next-item; do
            agent/scripts/check/check-task-contract.sh "$task"
          done
