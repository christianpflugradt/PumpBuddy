version: 1
source_of_truth: execution_config

id_format:
  plan_numeric_length: 3
  item_numeric_length: 2

runtime:
  dry_run: false

git:
  commit_enabled: true
  push_enabled: true
  pull_rebase_before_push: true

telemetry:
  enabled: true

release:
  trigger_on_finalize_accept: true
