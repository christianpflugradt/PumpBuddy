version: 1
kind: backlog_item
item:
  id: pb-030-item-03
  title: Implement side-aware workout-set API and persistence mapping
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Backend API and persistence layers correctly read/write side-aware set
    rows for bilateral and unilateral variants.
  rationale: Side-aware storage and transport are prerequisites for suggestion, resume,
    and UI flow changes.
scope:
  in:
  - Update backend request/response mapping to carry set-side information.
  - Update repository/service persistence logic to write/read side-aware workout-set
    entries.
  - Ensure canonical load semantics remain aligned with current TOTAL and PER_SIDE
    behavior.
  out:
  - Suggestion fallback logic changes.
  - Renderer UI behavior changes.
constraints:
- Preserve existing bilateral request/response behavior when set side is bilateral.
- Keep API/persistence transformations deterministic and side-safe.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/design/api-contract.yaml
  - agent/design/persistence-model.yaml
  - backend/src
  optional:
  - backend/tests
acceptance_criteria:
- id: ac-01
  statement: Backend accepts and returns side-aware workout-set data while persisting
    side-specific rows without breaking bilateral flows.
  verification:
    type: command
    command: make check
    expected: Quality gate passes with no regressions and includes side-aware API/persistence
      handling.
execution:
  plan_item_required: false
  risk_level: high
  boundary_impact:
  - backend/api
  - backend/service
  - backend/repository
handoff:
  review_focus:
  - Verify bilateral payload compatibility is preserved.
  - Verify side value validation and persistence mapping are complete on all save/update/read
    paths.
review_feedback:
- at: '2026-04-02T20:11:22Z'
  source: review_result
  notes: "findings:\n- criterion: Deterministic quality gates for the implementation\
    \ commit must pass for\n    acceptance of ac-01.\n  evidence: 'Quality gate failed\
    \ for source commit 3cebb93d97b944f8f978c03ad40b662a990c9ab0\n    at QUALITY_GATE_RUN=agent/scripts/check/verify-api-contract-alignment.sh\
    \ with\n    message \"API contract alignment failed: API surface changed without\
    \ updating agent/design/api-contract.yaml\".'\n  risk: Backend API and persisted\
    \ side-aware fields can diverge from the documented\n    API contract, causing\
    \ client integration mismatches and making side-aware behavior\n    non-authoritative\
    \ across implementation and contract artifacts.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: Acceptance criterion ac-01 is met; backend request/response mapping
      and persistence read/write paths now carry side-aware workout set fields (`set_side`,
      `set_tracking_mode`, `completed_sets`) while preserving bilateral compatibility.
    evidence: '`make check` passed end-to-end (backend + renderer quality gates/tests),
      and code/test inspection confirms side-aware handling on API mapping paths (`backend/src/api/models.rs`),
      persistence write/read paths (`backend/src/persistence/workouts.rs`, `backend/src/persistence/active_workouts.rs`),
      and regression coverage for bilateral payload flows (`backend/tests/api_workouts.rs`,
      `backend/tests/persistence_integration.rs`).'
    runtime_build_check: Runtime and deterministic quality checks passed in this review
      run via `make check` (including integration and coverage steps) with no regressions
      reported.
    residual_risk: Low residual risk remains around future contract drift if API surface
      evolves without synchronized design updates, but current side-aware API/persistence
      behavior and bilateral compatibility are covered by passing tests and quality
      checks.
