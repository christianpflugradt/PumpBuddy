version: 1
kind: backlog_item
item:
  id: pb-048-item-03
  title: Centralize performance tone classification utility
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Provide one shared backend utility for average-to-tone classification consumed
    by Exercises, Progress, and Workout Completion flows.
  rationale: Prevent threshold drift by enforcing a single implementation of RED/YELLOW/GREEN
    plus not-enough-data handling rules.
scope:
  in:
  - Extract or create shared classification helper(s) for score average and scored-entry-count
    checks.
  - Integrate existing backend flows to consume the shared helper instead of duplicated
    threshold checks.
  - Ensure threshold semantics stay aligned with plan constraints.
  out:
  - API contract path/schema changes.
  - Renderer screen or navigation updates.
constraints:
- Shared thresholds must remain <0.95 RED, 0.95-1.03 YELLOW, and >1.03 GREEN.
- Fewer than 3 scored entries must classify as not enough data in consumers that support
  gray grouping.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/strategy/test-strategy.yaml
  optional:
  - agent/strategy/engineering-guardrails.yaml
acceptance_criteria:
- id: ac-01
  statement: A single backend utility defines tone thresholds and not-enough-data
    gating and is used by all relevant performance consumers.
  verification:
    type: command
    command: make check-backend
    expected: Backend checks pass with tests proving shared utility usage in Exercises,
      Progress, and Workout Completion paths.
- id: ac-02
  statement: No duplicated threshold constants remain in touched backend paths after
    refactor.
  verification:
    type: command
    command: make check-backend
    expected: Quality checks pass and targeted tests confirm unchanged behavior for
      existing consumers.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: medium
  boundary_impact:
  - backend-shared-utilities
  - progress-performance-pipeline
  - workout-completion-pipeline
handoff:
  review_focus:
  - Backward compatibility of existing tone outputs after centralization.
  - Absence of duplicated threshold logic in touched modules.
review_feedback:
- at: '2026-04-22T09:58:38Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01 verification gate must pass in finalize\n\
    \  evidence: Finalize command `agent/scripts/task/review-item/finalize.sh 03 accept`\n\
    \    failed with exit code 41. The `agent/scripts/check/verify-api-contract-alignment.sh`\n\
    \    quality gate reported API surface changes in `backend/src/api/models.rs`\
    \ without\n    a corresponding `agent/design/api-contract.yaml` update in implementation\
    \ commit\n    `7a907df42ead441c9b265cac2ac5380597088edf`.\n  risk: Item cannot\
    \ be accepted while deterministic quality gates fail; proceeding\n    would violate\
    \ repository workflow policy and allow unverified API-contract alignment.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: ac-01 and ac-02 satisfied by a shared classifier in `backend/src/performance.rs`,
      consumer integration in Exercises (`backend/src/persistence/workouts.rs`) plus
      Progress/Workout Completion (`backend/src/api/models.rs`), and no duplicated
      threshold constants in touched backend runtime paths.
    evidence: Implementation commit `7a907df42ead441c9b265cac2ac5380597088edf` introduces
      shared utility usage with boundary-focused tests; follow-up commit `ccbbe4bc4e49f9d324f01c5eb72af5b90de7fb73`
      updates `agent/design/api-contract.yaml` to align documented tone semantics
      and clear prior contract-drift blocker.
    runtime_build_check: Deterministic quality gates will be executed via `agent/scripts/task/review-item/finalize.sh
      03 accept`, which runs `agent/scripts/check/run-quality-gate.sh` against the
      review item source commit.
    residual_risk: Low; residual exposure is limited to future drift if new consumers
      bypass `crate::performance`, mitigated by centralized constants plus existing
      boundary and integration coverage.
