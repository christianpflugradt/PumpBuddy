version: 1
kind: backlog_item
item:
  id: pb-032-item-03
  title: Propagate variant-derived repetition kind through backend domain and persistence
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Backend runtime behavior derives set repetition kind from selected variant
    metadata and persists kind/value consistently.
  rationale: Prevent client-only inference drift and keep server behavior authoritative
    for reps vs timed sets.
scope:
  in:
  - Update backend domain types and invariants for generic repetition kind/value on
    workout sets.
  - Adapt persistence mappers/repositories so read/write paths carry repetition kind/value
    end-to-end.
  - Ensure suggestion/bootstrap logic initializes `SECS` variants at `0:00` (0 seconds)
    without historical seconds defaulting.
  out:
  - HTTP handler payload shape changes and renderer UI control implementation.
constraints:
- Preserve existing load-input-mode, station-required/stationless, and set-tracking-mode
  invariants.
- Keep boundary validation explicit and deterministic for both `REPS` and `SECS`.
- Maintain active-workout resume compatibility.
inputs:
  required:
  - backend/src/domain.rs
  - backend/src/persistence/workouts.rs
  - backend/src/persistence/suggestions.rs
  optional:
  - backend/tests/persistence_integration.rs
  - agent/design/domain-model.yaml
acceptance_criteria:
- id: ac-01
  statement: Backend domain and persistence code no longer rely on set-level rep-only
    semantics and instead use repetition kind/value.
  verification:
    type: command
    command: rg "repetition_kind|repetition_value|REPS|SECS" backend/src/domain.rs
      backend/src/persistence/workouts.rs backend/src/persistence/suggestions.rs
    expected: Matches show repetition kind/value propagation across domain and persistence
      layers.
- id: ac-02
  statement: Persistence/integration behavior confirms variant-derived repetition
    kind and `SECS` suggestion initialization at zero seconds.
  verification:
    type: command
    command: make check-backend
    expected: Command exits successfully with updated backend tests.
execution:
  plan_item_required: false
  risk_level: high
  boundary_impact:
  - backend
  - database
handoff:
  review_focus:
  - Validate server-side authority for repetition-kind derivation and compatibility
    with existing invariants.
review_feedback:
- at: '2026-04-03T19:11:35Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01\n  evidence: In `backend/src/persistence/workouts.rs`,\
    \ `insert_workout_progress` first\n    parses `selected_variant_id` into `selected_variant_uuid`\
    \ for tolerant UUID handling,\n    but then queries `exercise_variants` with the\
    \ raw string via `WHERE id = $1::uuid`\n    using `.bind(selected_variant_id)`.\
    \ A malformed/non-UUID value that was previously\n    coerced to `NULL` for insert\
    \ can now still trigger a Postgres 22P02 cast error\n    on this query path.\n\
    \  risk: Active workout create/update can fail unexpectedly for malformed variant\
    \ ids\n    instead of following the existing tolerant optional-id behavior, introducing\n\
    \    a backend boundary regression unrelated to repetition semantics.n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: Acceptance criteria ac-01 and ac-02 are satisfied by current backend
      domain/persistence propagation and integration behavior.
    evidence: '`backend/src/persistence/workouts.rs` derives repetition kind from
      selected variant metadata using `selected_variant_uuid` and `exercise_variants.repetition_kind`,
      persists set values through `repetition_value`, and reads via `COALESCE(ws.repetition_value,
      ws.reps)` with `COALESCE(ev.repetition_kind, ''REPS'')`; `backend/src/persistence/suggestions.rs`
      scopes historical suggestions by repetition kind and initializes `SECS` defaults
      to zero; integration tests `create_workout_tolerates_malformed_optional_selection_uuids`
      and `secs_variant_suggestion_initializes_to_zero_seconds` in `backend/tests/persistence_integration.rs`
      verify malformed optional UUID tolerance and zero-second initialization for
      timed variants.'
    runtime_build_check: '`agent/scripts/task/review-item/finalize.sh 03 accept` completed
      successfully, including deterministic quality-gate execution via `agent/scripts/check/run-quality-gate.sh`.'
    residual_risk: Low residual risk remains around dual-column compatibility (`reps`
      plus `repetition_value`) during transition, but current mapping/coalesce paths
      preserve behavior and invariants.
