version: 1
kind: backlog_item
item:
  id: pb-025-item-03
  title: Allow stationless configured-gym workout progression with null set loads
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Backend workout validation and persistence accept stationless configured-gym
    variants with reps-only sets and null load fields end-to-end.
  rationale: Stationless variants must be executable in configured-gym mode without
    forcing station profile lookups or synthetic load values.
scope:
  in:
  - Update workout validation in backend/src/application/workouts.rs to skip station-profile
    load enforcement for stationless selections.
  - Update persistence write/read paths in backend/src/persistence/workouts.rs and
    backend/src/persistence/active_workouts.rs for nullable set load fields.
  - Keep station-based configured-gym validation strict for profile-constrained load
    values.
  out:
  - SQL schema/seed restructuring.
  - Renderer control visibility and interaction behavior.
constraints:
- Preserve free-mode behavior and fallback selection lock semantics.
inputs:
  required:
  - backend/src/application/workouts.rs
  - backend/src/persistence/workouts.rs
  - backend/src/persistence/active_workouts.rs
  - backend/init.sql
  optional:
  - agent/design/domain-model.yaml
acceptance_criteria:
- id: ac-01
  statement: Configured-gym stationless payloads with null set load values pass validation/persistence
    while off-profile station-based loads continue to fail.
  verification:
    type: command
    command: cargo test --manifest-path backend/Cargo.toml workouts
    expected: Backend workout validation and persistence tests pass for both stationless-null-load
      and station-based profile-load scenarios.
execution:
  plan_item_required: false
  risk_level: high
  boundary_impact:
  - backend/application-validation
  - backend/workout-persistence
handoff:
  review_focus:
  - Verify null-load handling does not panic or coerce nulls to defaults.
  - Verify station-based profile checks still run when station selection is present.
review_feedback:
- at: '2026-03-27T11:49:12Z'
  source: review_result
  notes: "findings:\n- criterion: Deterministic quality gate must pass before review\
    \ acceptance.\n  evidence: '`agent/scripts/task/review-item/finalize.sh 03 accept`\
    \ failed in quality\n    gate run `agent/scripts/run-quality.sh backend` with\
    \ a formatting diff in `backend/src/persistence/workouts.rs`\n    (mis-indented\
    \ `let set_rows = sqlx::query(` around line ~256).'\n  risk: Item cannot be accepted\
    \ while backend quality gate fails, which blocks deterministic\n    integration\
    \ even though targeted workouts tests pass.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: Acceptance criterion ac-01 is satisfied; configured-gym stationless
      workout paths allow null set loads while station-based configured-gym paths
      still enforce profile-constrained loads, and persistence read/write mappings
      keep set load fields nullable end-to-end.
    evidence: '`cargo test --manifest-path backend/Cargo.toml workouts` passed (17
      workout tests plus filtered integration/persistence targets), including `active_workout_selection_consistency_allows_null_set_loads_for_stationless_configured_gym`,
      `active_workout_selection_consistency_rejects_null_set_loads_for_station_based_configured_gym`,
      and `active_workout_selection_consistency_rejects_off_profile_set_loads_in_configured_gym`.'
    runtime_build_check: Deterministic quality gate execution is delegated to `agent/scripts/task/review-item/finalize.sh
      03 accept` and must pass for item finalization.
    residual_risk: Residual risk is low and limited to renderer submission states
      that may omit selection IDs; backend behavior in this scope is covered by targeted
      stationless/station-based validation tests and nullable persistence mapping.
