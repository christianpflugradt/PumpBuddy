version: 1
kind: backlog_item
item:
  id: pb-028-item-06
  title: Harden active workout response integration without contract drift
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Active workout application flow returns backend-computed suggested_set
    deterministically while preserving existing API contract semantics.
  rationale: Integration completion requires ensuring the deterministic backend logic
    reaches API responses with no contract drift.
scope:
  in:
  - validate application and API wiring in backend/src/application/workouts.rs and
    related handlers so suggested_set values are propagated unchanged from backend
    suggestion logic
  - verify no OpenAPI updates are required by comparing response shape behavior to
    current contract
  out:
  - introducing new active workout endpoints or payload fields
constraints:
- preserve contract-first discipline and avoid surface expansion without explicit
  need
- keep renderer free of set suggestion business logic
inputs:
  required:
  - backend/src/application/workouts.rs
  - backend/src/api/handlers/workouts.rs
  - agent/design/api-contract.yaml
  - backend/tests/api_workouts.rs
  optional:
  - renderer/src
acceptance_criteria:
- id: ac-01
  statement: Active workout API behavior exposes expected suggested_set values from
    backend logic with no schema drift from the canonical OpenAPI contract.
  verification:
    type: command
    command: cargo test --manifest-path backend/Cargo.toml --test api_workouts
    expected: API workout integration tests pass and no contract mismatch issues are
      introduced
execution:
  plan_item_required: false
  risk_level: low
  boundary_impact:
  - backend_application_api_integration
handoff:
  review_focus:
  - propagation of backend suggestions to API response payloads
  - contract consistency against agent/design/api-contract.yaml
review_feedback:
- at: '2026-03-28T19:35:15Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01\n  evidence: agent/scripts/task/review-item/finalize.sh\
    \ 06 accept failed the deterministic\n    quality gate because clippy flagged\
    \ backend/tests/api_workouts.rs:70 (`suggested_set_for_position<'a>`)\n    with\
    \ `needless_lifetimes` under `-D warnings`.\n  risk: The item cannot be finalized\n\
    \    as accepted while backend quality gates fail;\n    this blocks deterministic\
    \ review completion despite runtime test assertions passing.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'ac-01 satisfied: active-workout handlers return ActiveWorkoutResponse
      via active_workout_response(workout) without suggested_set transformation, and
      API integration tests assert suggested_set presence and stability on create/get/update
      flows.'
    evidence: backend/src/api/handlers/workouts.rs returns active_workout_response
      for GET/POST/PUT active-workout routes; backend/src/api/models.rs maps DomainActiveWorkoutExercise.suggested_set
      directly into ActiveWorkoutExerciseResponse.suggested_set; agent/design/api-contract.yaml
      includes suggested_set in ActiveWorkoutExerciseResponse with no schema edits
      in this item.
    runtime_build_check: cargo test --manifest-path backend/Cargo.toml --test api_workouts
      passed (6 passed, 0 failed) on 2026-03-28.
    residual_risk: 'Low: coverage confirms API-path propagation and contract alignment
      for exercised routes; residual risk is limited to unexercised edge payload combinations
      outside this item scope.'
