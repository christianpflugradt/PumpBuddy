version: 1
kind: backlog_item
item:
  id: pb-029-item-04
  title: Implement deterministic PER_SIDE load suggestion snapping
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Suggestion logic derives PER_SIDE candidates from historical total loads
    and snaps deterministically to nearest profile value.
  rationale: Preserves existing total-load history while producing stable per-side
    suggestions.
scope:
  in:
  - Update backend suggestion computation for PER_SIDE variants to divide historical
    totals by two before profile matching.
  - Implement deterministic nearest-value selection with lower-value tie-break.
  - Add focused tests for floating-point stability and tie cases.
  out:
  - Renderer input controls and labels.
  - Database schema or seed changes.
constraints:
- Historical workout_sets remain total-load source of truth.
- Floating-point drift must not alter selected profile value for equivalent inputs.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/design/workout-use-cases-to-domain-model.yaml
  - agent/strategy/test-strategy.yaml
  optional:
  - agent/design/domain-model.yaml
acceptance_criteria:
- id: ac-01
  statement: PER_SIDE suggestion code performs total-to-per-side conversion and deterministic
    nearest profile snapping.
  verification:
    type: command
    command: rg -n "PER_SIDE|nearest|tie|profile" backend/src
    expected: Matches identify explicit conversion and deterministic tie-break logic.
- id: ac-02
  statement: Automated tests validate nearest-value and tie-break behavior for PER_SIDE
    suggestions.
  verification:
    type: command
    command: cargo test --manifest-path backend/Cargo.toml suggestion
    expected: Suggestion-related tests pass including PER_SIDE coverage.
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - backend/suggestions
  - backend/workout-execution
handoff:
  review_focus:
  - Check tie-break behavior always prefers lower profile value.
  - Check conversion path always uses canonical historical totals as input.
review_feedback:
- at: '2026-03-29T14:59:38Z'
  source: review_result
  notes: "findings:\n- criterion: ac-02\n  evidence: Running `agent/scripts/task/review-item/finalize.sh\
    \ 04 accept` triggers\n    the required quality gate for source commit `db9d965636d2f28b40cc378c2f67d2c84ea890d0`,\n\
    \    which fails and reports a formatting diff in `backend/src/persistence/active_workouts.rs`\n\
    \    around the `snap_to_profile_load` call, so the deterministic gate does not\
    \ pass\n    for this implementation.\n  risk: The item is not releasable under\
    \ the workflow because acceptance finalization\n    is blocked by the mandatory\
    \ quality gate; downstream execution cannot reliably\n    advance with this review\
    \ marked accepted.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: AC-01 and AC-02 verified; PER_SIDE suggestions divide canonical
      historical totals by two before profile snapping, and nearest snapping remains
      deterministic with lower-value tie-break behavior.
    evidence: '`rg -n "PER_SIDE|nearest|tie|profile" backend/src` shows conversion
      in `backend/src/persistence/active_workouts.rs` (`Some("PER_SIDE") => suggestion.load_value
      / 2.0`) and deterministic snapping in `backend/src/persistence/suggestions.rs`
      where equal-distance ties resolve to lower (`if upper_distance + FLOAT_TOLERANCE
      < lower_distance { Some(upper) } else { Some(lower) }`), with explicit tests
      including `snap_to_profile_load_uses_lower_value_on_exact_tie` and `snap_to_profile_load_is_stable_for_float_adjacent_inputs`;
      `cargo test --manifest-path backend/Cargo.toml suggestion` passed (26 passed,
      0 failed including PER_SIDE mapping and suggestion suites).'
    runtime_build_check: 'Local runtime check passed: `cargo test --manifest-path
      backend/Cargo.toml suggestion` completed successfully with no failures, and
      the prior formatting-only quality gate blocker noted in review feedback is resolved
      by commit `7cc5b64` (`style(backend): format per-side suggestion snapping block`).'
    residual_risk: Low; scope is confined to suggestion/profile snapping logic with
      targeted deterministic and floating-point stability tests, and no unresolved
      acceptance gaps were observed.
