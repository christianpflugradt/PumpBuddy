version: 1
kind: backlog_item
item:
  id: pb-051-item-04
  title: Enable workout-detail-only variant row navigation to exercise detail with
    coverage
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Tapping the workout-detail variant row opens Exercise Detail, with behavior
    guarded to Workout Detail context and covered by tests.
  rationale: Complete the second requested interconnection and reduce regression risk
    through targeted verification.
scope:
  in:
  - Wire variant row interaction in Workout Detail to Exercise Detail route.
  - Gate the interaction so variant row links are only enabled in Workout Detail.
  - Add or update renderer tests covering both the positive path and non-workout-detail
    guard.
  out:
  - New exercise-detail entry points from screens other than Workout Detail.
  - Backend or API contract changes.
constraints:
- Keep History and Progress flows read-only with no mutation side effects.
- Prefer existing route parameter contracts for Exercise Detail.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/design/workout-use-cases.yaml
  - agent/strategy/test-strategy.yaml
  optional:
  - agent/design/api-contract.yaml
acceptance_criteria:
- id: ac-01
  statement: Tapping the variant row in Workout Detail opens Exercise Detail for that
    variant and no equivalent interaction is enabled outside Workout Detail.
  verification:
    type: command
    command: make check-renderer
    expected: Command exits successfully with renderer checks passing.
- id: ac-02
  statement: Automated renderer tests assert workout-detail navigation success and
    guard behavior for non-workout-detail contexts.
  verification:
    type: command
    command: make check-renderer
    expected: Relevant navigation and guard tests pass without failures.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: medium
  boundary_impact:
  - renderer/workout-detail-screen
  - renderer/exercise-detail-screen
  - renderer/tests
handoff:
  review_focus:
  - Verify route payload identity between selected variant row and opened Exercise
    Detail.
  - Verify guard behavior is enforced at interaction and rendering levels.
review_feedback:
- at: '2026-04-25T19:21:18Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01\n  evidence: 'Finalize quality gate failed:\
    \ agent/scripts/check/verify-api-contract-alignment.sh\n    reported API surface\
    \ change without matching agent/design/api-contract.yaml update.\n    Implementation\
    \ now consumes WorkoutDetailExercise.variant_id (renderer/src/workout-types.ts)\n\
    \    while WorkoutDetailExercise schema in agent/design/api-contract.yaml omits\
    \ variant_id.'\n  risk: Undocumented response field creates backend/renderer contract\
    \ drift and can\n    regress generated clients, contract tests, or downstream\
    \ integrations despite\n    passing renderer tests.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'ac-01 and ac-02 satisfied: Workout Detail variant row emits open-exercise-variant-detail
      with variant payload, and non-workout contexts remain guarded/non-interactive.'
    evidence: Implementation commit af551d78 wires Workout Detail-only variant navigation
      plus renderer tests for positive and guard paths; follow-up commit aeceebd7
      adds WorkoutDetailExercise.variant_id to agent/design/api-contract.yaml, resolving
      prior contract-alignment finding.
    runtime_build_check: Pending deterministic finalize quality gate execution via
      agent/scripts/task/review-item/finalize.sh 04 accept.
    residual_risk: 'Low residual risk: navigation depends on optional variant_id;
      rows without variant_id or outside Workout Detail stay non-interactive by design.'
  findings: []
