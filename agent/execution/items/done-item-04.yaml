version: 1
kind: backlog_item
item:
  id: pb-055-item-04
  title: Build renderer Gyms list navigation
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Add a top-level Gyms screen that lists gyms in the required two-line row
    layout and opens gym detail.
  rationale: Gives users an entry point to browse gym transparency data from the main
    navigation.
scope:
  in:
  - Add renderer state, data loading, and navigation action handling for the Gyms
    top-level screen.
  - Add the Gyms entry to existing side menus while preserving burger-menu behavior
    on list screens.
  - Render gyms in a history-style row with gym name first, then last visited and
    station count separated by a dot, plus a chevron drill-down affordance.
  - Open gym detail when a gym row is clicked.
  out:
  - Gym detail station/exercise sheet views.
  - Station detail screen and load profile popup.
  - Backend endpoint implementation.
constraints:
- Reuse existing Web Component and pb-ui-action patterns.
- Preserve existing workout, history, progress, exercises, settings, about, and logout
  navigation behavior.
- Keep user-facing copy in English.
inputs:
  required:
  - agent/execution/plan.yaml
  - renderer/src/workout-types.ts
  - renderer/src/workout-api.ts
  - renderer/src/openapi-contract.ts
  - renderer/src/pb-app-root.ts
  - renderer/src/workout-controller-navigation.ts
  - renderer/src/workout-controller-screen-data.ts
  - renderer/src/pb-history-screen.ts
  - renderer/src/styles.scss
  optional:
  - renderer/src/pb-history-screen.test.ts
  - renderer/src/pb-app-root.test.ts
  - renderer/src/workout-controller.test.ts
  - renderer/src/workout-api.test.ts
acceptance_criteria:
- id: ac-01
  statement: Users can navigate to a Gyms screen from side menus and see gym rows
    with name, last-visited text, station count, and chevron drill-down affordance.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer checks pass with component or controller coverage for Gyms
      navigation and row rendering.
- id: ac-02
  statement: Clicking a gym row transitions to a gym detail view state without losing
    the selected gym id.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer checks pass with navigation coverage for opening gym detail
      from the Gyms list.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: medium
  boundary_impact:
  - renderer-navigation
  - renderer-state
  - renderer-api-client
  - renderer-ui
handoff:
  review_focus:
  - Side-menu additions are consistent across list screens and do not break existing
    routes.
  - Gym row markup follows the existing history-row affordance closely enough to feel
    native.
review_feedback:
- at: '2026-05-29T20:04:37Z'
  source: review_result
  notes: "findings:\n- criterion: quality gate api_contract_alignment\n  evidence:\
    \ 'Accept finalization failed before item completion: agent/scripts/check/verify-api-contract-alignment.sh\n\
    \    flagged source commit 90e91cd9 because renderer/src/workout-api.ts changed\
    \ without\n    agent/design/api-contract.yaml in the same implementation commit.'\n\
    \  risk: The item cannot be accepted until the API-surface change is paired with\
    \ canonical\n    contract evidence in agent/design/api-contract.yaml, so corrective\
    \ implementation\n    must address the contract-alignment gate before review can\
    \ pass.\n  requires_api_contract_update: true\n"
  findings:
  - criterion: quality gate api_contract_alignment
    evidence: 'Accept finalization failed before item completion: agent/scripts/check/verify-api-contract-alignment.sh
      flagged source commit 90e91cd9 because renderer/src/workout-api.ts changed without
      agent/design/api-contract.yaml in the same implementation commit.'
    risk: The item cannot be accepted until the API-surface change is paired with
      canonical contract evidence in agent/design/api-contract.yaml, so corrective
      implementation must address the contract-alignment gate before review can pass.
    requires_api_contract_update: true
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'ac-01 and ac-02 are met: Gyms is reachable from side menus, renders
      required row metadata with a chevron, and row clicks transition to gym-detail
      while preserving gymId. The returned api_contract_alignment finding is addressed
      by the canonical API contract note in agent/design/api-contract.yaml.'
    evidence: Reviewed current main after fd4560f6. Renderer state and navigation
      include gyms and gym-detail view states, loadGymSummaries uses /api/gyms through
      parseGymSummaries, pb-gyms-screen renders name plus last-visited and station-count
      metadata with the history-row chevron, controller tests cover loading and open-gym-detail,
      and agent/design/api-contract.yaml now records the Gyms renderer navigation
      alignment with the generated GymSummary boundary.
    runtime_build_check: Corrective implementation evidence shows make refresh-api-clients
      and make check-renderer passed, and the targeted API contract alignment check
      passed after adding the contract note; accept finalize will run deterministic
      quality gates for the source commit.
    residual_risk: 'Low: gym-detail remains an intentional handoff placeholder because
      station and sheet detail views are out of scope for this item.'
  findings: []
