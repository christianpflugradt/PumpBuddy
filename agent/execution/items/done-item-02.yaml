version: 1
kind: backlog_item
item:
  id: pb-029-item-02
  title: Add load_input_mode column and TOTAL defaults
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Persistence layer supports ExerciseVariant.load_input_mode with backward-compatible
    TOTAL defaults.
  rationale: Enables PER_SIDE modeling without breaking existing variants, migrations,
    or seeds.
scope:
  in:
  - Add exercise_variants.load_input_mode schema support with TOTAL and PER_SIDE constraints.
  - Backfill migrated rows to TOTAL and set seed data defaults to TOTAL.
  - Update persistence models/mappings that materialize exercise variant rows.
  out:
  - API payload shape changes.
  - Renderer interaction changes.
constraints:
- Existing and seeded variants must remain valid without manual data repair.
- Migration must be deterministic and safe for existing production-like data.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/design/persistence-model.yaml
  - agent/strategy/engineering-guardrails.yaml
  - agent/strategy/test-strategy.yaml
  optional:
  - agent/design/domain-model.yaml
acceptance_criteria:
- id: ac-01
  statement: Migration and seed sources encode load_input_mode with TOTAL defaults
    and PER_SIDE as allowed value.
  verification:
    type: command
    command: rg -n "load_input_mode|TOTAL|PER_SIDE" backend/migrations backend/src
      backend/seed.sql
    expected: Matches show schema, defaults/backfill behavior, and seed compatibility.
- id: ac-02
  statement: Persistence tests cover migration/default behavior for pre-existing exercise
    variants.
  verification:
    type: command
    command: cargo test --manifest-path backend/Cargo.toml load_input_mode
    expected: Relevant backend tests pass and validate TOTAL default compatibility.
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - backend/database
  - backend/persistence
  - seed-data
handoff:
  review_focus:
  - Validate non-null/default behavior for migrated rows.
  - Validate enum/check constraints prevent invalid load_input_mode values.
review_feedback:
- at: '2026-03-29T14:07:41Z'
  source: review_result
  notes: "findings:\n- criterion: quality_gate.backend_compile\n  evidence: '`agent/scripts/task/review-item/finalize.sh\
    \ 02 accept` failed during\n    `agent/scripts/run-quality.sh backend` with Rust\
    \ E0063 compile errors after OpenAPI\n    generation, including missing `load_input_mode`\
    \ in `PlanExerciseOptionSummaryResponse`/`ActiveWorkoutExerciseResponse`\n   \
    \ and missing `load_value_per_side` in active-workout set response/input mappings.'\n\
    \  risk: Backend cannot pass quality gate or compile cleanly against the generated\n\
    \    API model contract, so this item is not releasable in its current state.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: AC-01 and AC-02 satisfied for item-02 scope. Schema/seed sources encode load_input_mode with deterministic TOTAL defaults and PER_SIDE as an allowed value, and persistence tests validate backfill/default and invalid-value rejection behavior.
    evidence: Verified with `rg -n "load_input_mode|TOTAL|PER_SIDE" runtime/database/00-schema.sql runtime/database/10-seed-dev.sql backend/src` showing schema default/backfill/check-constraint coverage, explicit TOTAL seed defaults, and persistence row materialization updates; confirmed `backend/tests/persistence_integration.rs` covers `load_input_mode_backfills_preexisting_variants_to_total` and `load_input_mode_rejects_invalid_variant_values`.
    runtime_build_check: '`cargo test --manifest-path backend/Cargo.toml load_input_mode` passed (`2 passed; 0 failed`) for targeted persistence compatibility checks. The broader backend quality-gate compile failure references API-model fields owned by pb-029-item-03 and is tracked as a cross-item dependency.'
    residual_risk: Moderate and explicit. Backend/OpenAPI compile parity remains pending until pb-029-item-03 (API payload/model alignment) lands; item-02 persistence/schema behavior is complete and valid within its declared in/out scope boundaries.
