version: 1
kind: backlog_item
item:
  id: pb-048-item-02
  title: Implement backend variant aggregation endpoint
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Deliver backend endpoint logic that returns deterministic 30-day variant
    performance rows with selected-station scoring and variant-wide session counts.
  rationale: Concentrate business logic in the backend so renderer consumers remain
    thin and deterministic.
scope:
  in:
  - Implement endpoint handler and service/query flow for exercises performance data
    over the last 30 days.
  - Select per-variant station stream by highest in-window scored sample count with
    most-recent tie-break behavior.
  - Compute selected-station average inputs and variant-wide session counts in one
    backend-owned response.
  out:
  - Shared threshold utility extraction across existing Progress and Workout Completion
    paths.
  - Renderer page wiring and menu entry changes.
constraints:
- Deterministic ordering and grouping are required to avoid UI shuffle between runs.
- Predecessor linkage outside the 30-day window may be used only for score validity,
  not for in-window counts.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/design/domain-model.yaml
  - agent/design/persistence-model.yaml
  - agent/design/workout-use-cases-to-domain-model.yaml
  optional:
  - agent/design/workout-use-cases.yaml
acceptance_criteria:
- id: ac-01
  statement: Endpoint output includes grouped rows by performance tone with each row
    containing variant metadata, last-workout first-set summary fields, selected-station
    average inputs, and variant-wide session count.
  verification:
    type: command
    command: make check-backend
    expected: Backend quality suite passes with endpoint tests covering grouping and
      row shape.
- id: ac-02
  statement: Station selection logic is deterministic per variant and follows max
    scored samples in-window with most-recent tie-break behavior.
  verification:
    type: command
    command: make check-backend
    expected: Backend tests validate station selection and tie-break outcomes.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: high
  boundary_impact:
  - backend-api
  - backend-service-layer
  - backend-query-layer
handoff:
  review_focus:
  - Deterministic station stream selection and tie-break implementation.
  - Correct separation between selected-station averages and variant-wide session
    counts.
review_feedback:
- at: '2026-04-22T05:56:27Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01 verification gate must pass in finalize\n\
    \  evidence: Finalize command `agent/scripts/task/review-item/finalize.sh 02 accept`\n\
    \    failed with exit code 41 in `agent/scripts/check/verify-api-contract-alignment.sh`\n\
    \    for source commit `cce5434fe35ae7db32644e7328d81e4bcea25ab3`, reporting API-surface\n\
    \    file changes under `backend/src/api/*` without a corresponding `agent/design/api-contract.yaml`\n\
    \    update in the same commit.\n  risk: Item cannot be accepted while deterministic\
    \ quality gates fail; proceeding\n    would bypass repository workflow policy\
    \ and allow unverified API-contract alignment.\n"
- at: '2026-04-22T06:02:54Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01 verification gate must pass in finalize\n\
    \  evidence: Since the prior return at `5b14252c`, the review handoff commit `a6f15275`\n\
    \    only moved workflow artifacts (`agent/execution/items/*`, `agent/execution/plans/*`,\n\
    \    `agent/execution/telemetry.yaml`, `agent/execution/workflow-state.yaml`)\
    \ and did\n    not add a new backend implementation commit that resolves the earlier\
    \ quality-gate\n    failure from `cce5434fe35ae7db32644e7328d81e4bcea25ab3` (`verify-api-contract-alignment.sh`\n\
    \    required an `agent/design/api-contract.yaml` update alongside API-surface\
    \ changes).\n  risk: Accepting without a corrective implementation commit would\
    \ leave the prior\n    acceptance blocker unresolved and allow the workflow to\
    \ advance without evidence\n    that the API-contract alignment requirement has\
    \ been satisfied.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: ac-01, ac-02
    evidence: Implementation commit `cce5434fe35ae7db32644e7328d81e4bcea25ab3` adds
      `/api/workouts/exercises-performance` handler/service/query flow and deterministic
      grouping/row shaping, including backend integration tests in `backend/tests/api_workouts.rs`
      that verify grouped tone buckets, row fields, selected-station averages, and
      variant-wide session counts. Corrective commit `e2746a5b13a17b8b875a4ee3113cf7caa453fc00`
      updates `agent/design/api-contract.yaml` alongside backend API mapping changes
      and adds row/group tone-alignment assertions in `backend/src/api/models.rs`.
    runtime_build_check: pending finalize
    residual_risk: Low. Deterministic ordering and station tie-break behavior are
      covered by integration tests; residual risk is limited to future schema changes
      that might bypass contract-alignment guardrails.
