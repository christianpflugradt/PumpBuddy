version: 1
kind: backlog_item
item:
  id: pb-027-item-11
  title: Simplify Workout Header to 3-Line Hierarchy
  status_hint: done
  source:
    type: stakeholder_feedback
    reference: agent/tmp/workout_ui_spec.md
intent:
  outcome: Reduce header cognitive load with a compact 3-line information hierarchy.
  rationale: The current ordering still splits context in a way that weakens scan
    clarity.
scope:
  in:
  - "render workout header in this order: Exercise Name, Variant, Plan plus position\
    \ (for example \"Leg Day \xB7 2/6\")"
  - remove redundant hierarchy wrappers where unnecessary
  - keep mandatory workout context information present
  out:
  - changes to workout navigation logic
  - changes to completion/start screen layouts beyond shared spacing side effects
constraints:
- preserve all required context content while reordering hierarchy
- keep copy concise and english
inputs:
  required:
  - agent/tmp/workout_ui_spec.md
  - renderer/src/workout-render.ts
  - renderer/src/styles.scss
  - renderer/src/workout-render.test.ts
  optional:
  - agent/tmp/pumpbuddy_workout_redesign_revised.md
acceptance_criteria:
- id: ac-01
  statement: Workout header renders as a compact 3-line hierarchy with required context
    preserved.
  verification:
    type: command
    command: cd renderer && npm run vitest:run -- src/workout-render.test.ts
    expected: command exits 0 and tests assert new header order and required fields
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - renderer/src/workout-render.ts
  - renderer/src/styles.scss
  - renderer/src/workout-render.test.ts
handoff:
  review_focus:
  - verify header order is Exercise, Variant, Plan+Position
  - verify no required context is dropped after compaction
review_feedback:
- at: '2026-03-28T15:37:40Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01 verification is incomplete because repository-level\
    \ renderer quality-gate\n    tests do not pass after the header change.\n  evidence:\
    \ \"Running finalize quality gate (`agent/scripts/run-quality.sh renderer`)\\\n\
    \    \\ fails with 7 failing tests in renderer/src/app.test.ts. Failures assert\
    \ removed\\\n    \\ legacy header semantics (for example expected /Exercise 1\
    \ of 2/, /Exercise 2\\\n    \\ of 2/, and presence/order including `exercise-step-copy`)\
    \ while current output\\\n    \\ renders the new compact line `Push Day at Forge\
    \ Downtown \\xB7 1/2` without those\\\n    \\ legacy tokens.\"\n  risk: Merging\
    \ this item as accepted would leave the renderer test suite red, masking\n   \
    \ regressions and blocking deterministic quality-gate enforcement for subsequent\n\
    \    work.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'ac-01 met: workout header renders in compact three-line order (Exercise
      Name, Variant, Plan+Position) while retaining required workout context.'
    evidence: Verified in renderer/src/workout-render.ts header markup and renderer/src/workout-render.test.ts
      assertions, including explicit order check for `exercise-name`, `exercise-variant-label`,
      and `plan-label` plus required context content.
    runtime_build_check: 'Passed: `cd renderer && npm run vitest:run -- src/workout-render.test.ts`
      (12 tests passed, exit 0) on 2026-03-28.'
    residual_risk: 'Low residual risk: acceptance coverage is scoped to workout header
      behavior in targeted renderer tests; broader suite compatibility is delegated
      to finalize quality gates.'
