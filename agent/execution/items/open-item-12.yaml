version: 1
kind: backlog_item
item:
  id: pb-037-item-12
  title: Align REPS label and centered guidance on one row
  status_hint: open
  source:
    type: finalize_return
    reference: agent/tmp/finalize-plan-findings.yaml
intent:
  outcome: Render the static REPS label and centered guidance text in the same horizontal
    row above reps controls.
  rationale: Split-line layout increases vertical spacing above minus/input controls
    and does not match expected side-by-side row alignment.
scope:
  in:
  - Adjust Current Set reps row markup/CSS so REPS label and centered guidance share
    one row with two side-by-side cells.
  - Preserve existing minus/input/plus control order and behavior below that row.
  out:
  - Changes to guidance selection logic and text content.
  - Global typography or unrelated layout refactors.
constraints:
- Keep guidance centered within its cell while REPS remains left-aligned.
- Do not regress hide/show behavior when guidance is absent.
inputs:
  required:
  - agent/design/workout-capabilities.yaml
  - agent/execution/plan.yaml
  optional: []
acceptance_criteria:
- id: ac-01
  statement: Current Set reps row shows REPS and guidance in one shared row above
    controls, avoiding the extra vertical gap caused by separate lines.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer checks pass with coverage asserting one-row label/guidance
      layout behavior.
execution:
  plan_item_required: true
  risk_level: medium
  boundary_impact:
  - renderer
handoff:
  review_focus:
  - Verify label/guidance row structure and spacing relative to minus/input/plus controls.
