version: 1
kind: backlog_item
item:
  id: pb-041-item-04
  title: Deliver settings profile UI for registration date and favorite gym editing
  status_hint: open
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Settings screen shows human-readable registration date and supports favorite
    gym read-only plus edit/save/discard flow.
  rationale: Completes core user-facing preference management behavior on the settings
    page.
scope:
  in:
  - Render registration date in human-readable form on existing settings screen.
  - Add favorite gym settings section with read-only display and pen-icon edit mode
    using dropdown selection.
  - Implement save/discard behavior aligned with current display-name editing interaction.
  out:
  - Workout-start favorite gym preselection changes.
  - Password section interaction and validation flow.
constraints:
- Keep implementation aligned with existing web-components patterns.
- Reuse authenticated settings API contract without bypassing generated types.
- Preserve existing settings behavior outside targeted sections.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/design/api-contract.yaml
  - agent/strategy/tech-stack.yaml
  optional:
  - agent/design/workout-capabilities.yaml
acceptance_criteria:
- id: ac-01
  statement: Settings UI displays formatted registration date and favorite gym edit/save/discard
    behavior using authenticated data.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer checks pass with new settings component behavior covered.
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - renderer-settings-ui
  - renderer-settings-state
handoff:
  review_focus:
  - UI state transitions between read-only and edit modes.
  - Registration date formatting and favorite gym value persistence UX.
review_feedback:
- at: '2026-04-13T14:46:56Z'
  source: review_result
  notes: "findings:\n- criterion: ac-01\n  evidence: Deterministic finalize quality\
    \ gate for accept (`agent/scripts/task/review-item/finalize.sh\n    04 accept`)\
    \ ran `make check-renderer` successfully but then failed `agent/scripts/check/verify-api-contract-alignment.sh`\n\
    \    with \"API surface changed without updating agent/design/api-contract.yaml\"\
    .\n  risk: Item cannot be accepted while required quality gates fail; shipping\
    \ in this\n    state risks renderer/backend contract drift and regressions in\
    \ authenticated settings\n    behavior.\n"
review_result:
  outcome: return
  findings:
  - criterion: ac-01
    evidence: Deterministic finalize quality gate for accept (`agent/scripts/task/review-item/finalize.sh
      04 accept`) ran `make check-renderer` successfully but then failed `agent/scripts/check/verify-api-contract-alignment.sh`
      with "API surface changed without updating agent/design/api-contract.yaml".
    risk: Item cannot be accepted while required quality gates fail; shipping in this
      state risks renderer/backend contract drift and regressions in authenticated
      settings behavior.
