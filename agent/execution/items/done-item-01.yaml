version: 1
kind: backlog_item
item:
  id: pb-042-item-01
  title: Add About Screen Navigation Entry Points
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Wire the renderer About screen into sidebar and burger-menu navigation
    with parity to existing screens.
  rationale: Users must be able to discover and open About from all standard navigation
    paths.
scope:
  in:
  - Add an About navigation entry in the sidebar directly above Logout.
  - Register About in the renderer screen routing/navigation state.
  - Ensure burger-menu flows and keyboard navigation can open About the same way as
    other screens.
  out:
  - Rendering final About content details beyond minimal screen shell.
  - Backend metadata sourcing and persistence changes.
constraints:
- Preserve existing renderer navigation patterns and accessibility behavior.
- Keep renderer business-logic free and defer metadata ownership to backend/Rust layers.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/strategy/tech-stack.yaml
  - agent/strategy/engineering-guardrails.yaml
  optional:
  - agent/design/api-contract.yaml
acceptance_criteria:
- id: ac-01
  statement: Sidebar shows About directly above Logout and selecting it routes to
    the About screen.
  verification:
    type: command
    command: rtk make check-renderer
    expected: Renderer checks pass with About navigation integration.
- id: ac-02
  statement: Burger-menu and keyboard navigation can reach About using existing navigation
    parity patterns.
  verification:
    type: command
    command: rtk make check-renderer
    expected: Renderer checks confirm no regressions in menu/navigation behavior.
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - renderer/navigation
  - renderer/screen-routing
handoff:
  review_focus:
  - About entry order relative to Logout.
  - Burger-menu and keyboard parity with existing screen navigation.
review_feedback:
- at: '2026-04-14T04:38:28Z'
  source: review_result
  notes: "findings:\n- criterion: quality-gate api_contract_alignment (required deterministic\
    \ gate during\n    review finalize)\n  evidence: 'Finalize quality gate failed\
    \ for source commit b2dbda1 with `API contract\n    alignment failed: API surface\
    \ changed without updating agent/design/api-contract.yaml`;\n    changed files\
    \ include `renderer/src/workout-types.ts`, which is explicitly treated\n    as\
    \ API surface by `agent/scripts/check/verify-api-contract-alignment.sh`.'\n  risk:\
    \ Merged state would violate repository API-contract/source alignment policy\n\
    \    and can leave design contract drifted from the renderer/backend API surface\
    \ definition.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: ac-01 and ac-02 satisfied; About is present directly above Logout
      in start/settings side-menu markup and navigation actions route to/from About
      with existing parity rules.
    evidence: 'Renderer wiring and tests cover the required behavior: About entry
      placement and action emission in pb-start-screen/pb-settings-screen tests, About
      screen shell + burger-menu behavior in pb-about-screen tests, and controller
      routing parity in workout-controller test; prior API-contract alignment return
      finding was addressed in source commit 7eaf9fd by updating agent/design/api-contract.yaml
      and tightening AboutMetadata.channel typing.'
    runtime_build_check: make check-renderer passed locally in this review run (lint,
      unit tests, coverage, UI smoke checks, and renderer build completed successfully).
    residual_risk: Low; acceptance scope is limited to navigation entry points and
      parity behavior, with minor residual UI-ordering regression risk on future menu
      refactors.
