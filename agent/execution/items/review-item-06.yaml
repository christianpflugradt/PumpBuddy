version: 1
kind: backlog_item
item:
  id: pb-039-item-06
  title: Align design artifacts with gym-agnostic option model
  status_hint: review
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Domain, persistence, and API design artifacts consistently describe gym-agnostic
    plan exercise options and compatibility-based realizability.
  rationale: Design documentation must reflect the updated source-of-truth model used
    by implementation.
scope:
  in:
  - Update relevant sections in `agent/design/domain-model.yaml`.
  - Update relevant sections in `agent/design/persistence-model.yaml`.
  - Update relevant sections in `agent/design/api-contract.yaml` and cross-reference
    use-case artifacts when needed.
  out:
  - New feature scope beyond plan-defined realizability and blocked-start clarity.
  - Workflow policy/process updates.
constraints:
- Keep terminology consistent with existing glossary/use-case artifacts.
- Avoid contract churn beyond blocked-start clarity and option source-of-truth updates.
inputs:
  required:
  - agent/design/domain-model.yaml
  - agent/design/persistence-model.yaml
  - agent/design/api-contract.yaml
  - agent/design/workout-use-cases.yaml
  optional:
  - agent/design/glossary.yaml
  - agent/design/workout-use-cases-to-domain-model.yaml
acceptance_criteria:
- id: ac-01
  statement: Design artifacts consistently encode gym-agnostic options plus compatibility-derived
    configured-gym realizability and blocked-start semantics.
  verification:
    type: command
    command: make check
    expected: Repository quality checks pass after design and implementation alignment
      updates.
execution:
  plan_item_required: false
  risk_level: low
  boundary_impact:
  - design documentation
  - cross-layer contract clarity
handoff:
  review_focus:
  - Verify no artifact still treats `plan_exercise_options` as gym-owned.
  - Verify blocked-start semantics match backend and renderer expectations.
review_feedback: []
review_result: null
