feat(renderer): migrate renderer tests and coverage to Vitest

Replace Node's experimental test runner usage with Vitest for unit tests and
coverage. Add `vitest` devDependency, minimal `vitest.config.ts`, and update
test wrapper scripts to invoke the Vitest CLI while preserving the existing
coverage badge generation.

Files: renderer/package.json, renderer/scripts/run-tests.mjs,
renderer/scripts/run-coverage.mjs, renderer/vitest.config.ts
