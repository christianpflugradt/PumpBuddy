ci(release): run semantic-release from repo-managed toolchain

Replace floating `npx -p` invocation in the GitHub Actions release workflow
with an install of the repository-managed toolchain (`npm ci`) and execution
of the pinned `semantic-release` binary via the `npm run release` script.

Files: .github/workflows/release.yml, RELEASES.md
