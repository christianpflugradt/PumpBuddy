import type { ConfiguratorExerciseVariant, ConfiguratorExerciseVariantCreateRequest, ConfiguratorExerciseVariantUpdateRequest } from "./workout-contract";
import { TextInputBinding } from "./text-input-binding";

export const pbConfiguratorExerciseVariantEditorScreenTag = "pb-configurator-exercise-variant-editor-screen";
export type ConfiguratorExerciseVariantEditorScreenState = { exerciseId: string; exerciseName: string; variant: ConfiguratorExerciseVariant | null; isLoading: boolean; errorMessage: string | null };
type SaveResult = { ok: boolean; errorMessage?: string };
type SaveDetail = { action: "save-configurator-exercise-variant"; payload: { exerciseId: string; variantId: string | null; request: ConfiguratorExerciseVariantCreateRequest | ConfiguratorExerciseVariantUpdateRequest }; respond: (result: SaveResult) => void };
type DeleteDetail = { action: "delete-configurator-exercise-variant"; payload: { exerciseId: string; variantId: string }; respond: (result: SaveResult) => void };
const escapeHtml = (value: string): string => value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
const statusLabel = (value: ConfiguratorExerciseVariant["status"]): string => value === "new" ? "Draft" : value === "active" ? "Active" : "Inactive";

class PbConfiguratorExerciseVariantEditorScreenElement extends HTMLElement {
  #state: ConfiguratorExerciseVariantEditorScreenState = { exerciseId: "", exerciseName: "", variant: null, isLoading: false, errorMessage: null };
  #loadedKey: string | null = null; #name = ""; #requiresStation = true; #loadInputMode: "TOTAL" | "PER_SIDE" = "TOTAL"; #setTrackingMode: "BILATERAL" | "UNILATERAL" = "BILATERAL"; #repetitionKind: "REPS" | "SECS" = "REPS"; #touched = false; #isSaving = false; #isDeleting = false; #submitError: string | null = null;
  #textInput = new TextInputBinding(this, ({ field, value }) => this.#onTextInput(field, value));
  connectedCallback(): void { this.#render(); this.addEventListener("click", this.#onClick); this.#textInput.connect(); this.addEventListener("change", this.#onChange); }
  disconnectedCallback(): void { this.removeEventListener("click", this.#onClick); this.#textInput.disconnect(); this.removeEventListener("change", this.#onChange); }
  set state(value: ConfiguratorExerciseVariantEditorScreenState) { this.#state = value; const key = value.variant ? `edit:${value.variant.id}` : `create:${value.exerciseId}`; if (key !== this.#loadedKey) { this.#loadedKey = key; const v = value.variant; this.#name = v?.name ?? ""; this.#requiresStation = v?.requires_station ?? true; this.#loadInputMode = v?.load_input_mode ?? "TOTAL"; this.#setTrackingMode = v?.set_tracking_mode ?? "BILATERAL"; this.#repetitionKind = v?.repetition_kind ?? "REPS"; this.#touched = false; this.#isSaving = false; this.#isDeleting = false; this.#submitError = null; } this.#render(); }
  get state(): ConfiguratorExerciseVariantEditorScreenState { return this.#state; }
  #isHistorical = (): boolean => this.#state.variant?.status === "active" || this.#state.variant?.status === "inactive";
  #nameError = (): string | null => this.#name.trim() ? null : "Name is required.";
  #emit(action: string): void { this.dispatchEvent(new CustomEvent("pb-ui-action", { bubbles: true, composed: true, detail: { action } })); }
  #onTextInput = (field: string, value: string): void => { if (field !== "name") return; this.#name = value; this.#touched = true; this.#submitError = null; this.querySelector(".configurator-gym-field-error, .start-error")?.remove(); const saveButton = this.querySelector<HTMLButtonElement>('[data-ui-action="save-configurator-exercise-variant"]'); if (saveButton) saveButton.disabled = this.#isSaving || this.#isDeleting || !!this.#nameError(); };
  #onChange = (event: Event): void => { if (this.#isHistorical()) return; const target = event.target; if (!(target instanceof HTMLInputElement)) return; if (target.dataset.field === "requires-station") this.#requiresStation = target.checked; if (target.dataset.field === "load-input-mode" && (target.value === "TOTAL" || target.value === "PER_SIDE")) this.#loadInputMode = target.value; if (target.dataset.field === "set-tracking-mode" && (target.value === "BILATERAL" || target.value === "UNILATERAL")) this.#setTrackingMode = target.value; if (target.dataset.field === "repetition-kind" && (target.value === "REPS" || target.value === "SECS")) this.#repetitionKind = target.value; this.#touched = true; this.#render(); };
  #onClick = (event: Event): void => { const target = event.target; if (!(target instanceof Element)) return; const action = target.closest<HTMLElement>("[data-ui-action]")?.dataset.uiAction; if (!action) return; if (action === "navigate-back-from-configurator-exercise-variant-detail") { this.#emit(action); return; } if (action === "save-configurator-exercise-variant") { const error = this.#nameError(); if (error) { this.#touched = true; this.#submitError = error; this.#render(); return; } if (this.#isSaving || this.#isDeleting) return; const structural = { requires_station: this.#requiresStation, load_input_mode: this.#loadInputMode, set_tracking_mode: this.#setTrackingMode, repetition_kind: this.#repetitionKind }; const request = this.#state.variant && this.#isHistorical() ? { name: this.#name.trim() } : { name: this.#name.trim(), ...structural }; this.#isSaving = true; this.#render(); this.dispatchEvent(new CustomEvent<SaveDetail>("pb-ui-action", { bubbles: true, composed: true, detail: { action, payload: { exerciseId: this.#state.exerciseId, variantId: this.#state.variant?.id ?? null, request }, respond: (result) => { this.#isSaving = false; this.#submitError = result.ok ? null : result.errorMessage ?? null; this.#render(); } } })); return; } if (action === "delete-configurator-exercise-variant") { const variant = this.#state.variant; if (!variant || variant.status !== "new" || this.#isSaving || this.#isDeleting) return; this.#isDeleting = true; this.#render(); this.dispatchEvent(new CustomEvent<DeleteDetail>("pb-ui-action", { bubbles: true, composed: true, detail: { action, payload: { exerciseId: this.#state.exerciseId, variantId: variant.id }, respond: (result) => { this.#isDeleting = false; this.#submitError = result.ok ? null : result.errorMessage ?? null; this.#render(); } } })); } };
  #render(): void { const v = this.#state.variant; const historical = this.#isHistorical(); const disabled = this.#isSaving || this.#isDeleting || !!this.#nameError(); const field = (label: string, body: string) => `<label class="configurator-gym-field"><span class="configurator-gym-field-label">${label}</span>${body}</label>`; const choice = (label: string, name: string, value: string, selected: boolean) => `<label><input type="radio" data-field="${name}" name="${name}" value="${value}" ${selected ? "checked" : ""} ${historical || this.#isSaving || this.#isDeleting ? "disabled" : ""} /> ${label}</label>`; const structural = historical ? `<dl class="configurator-load-profile-metadata"><div><dt>Requires station</dt><dd>${this.#requiresStation ? "Yes" : "No"}</dd></div><div><dt>Load input</dt><dd>${this.#loadInputMode === "TOTAL" ? "Total" : "Per side"}</dd></div><div><dt>Set tracking</dt><dd>${this.#setTrackingMode === "BILATERAL" ? "Bilateral" : "Unilateral"}</dd></div><div><dt>Repetition kind</dt><dd>${this.#repetitionKind === "REPS" ? "Reps" : "Seconds"}</dd></div></dl>` : `${field("Requires station", `<input type="checkbox" data-field="requires-station" ${this.#requiresStation ? "checked" : ""} ${this.#isSaving || this.#isDeleting ? "disabled" : ""} />`)}${field("Load input", choice("Total", "load-input-mode", "TOTAL", this.#loadInputMode === "TOTAL") + choice("Per side", "load-input-mode", "PER_SIDE", this.#loadInputMode === "PER_SIDE"))}${field("Set tracking", choice("Bilateral", "set-tracking-mode", "BILATERAL", this.#setTrackingMode === "BILATERAL") + choice("Unilateral", "set-tracking-mode", "UNILATERAL", this.#setTrackingMode === "UNILATERAL"))}${field("Repetition kind", choice("Reps", "repetition-kind", "REPS", this.#repetitionKind === "REPS") + choice("Seconds", "repetition-kind", "SECS", this.#repetitionKind === "SECS"))}`; const body = this.#state.isLoading ? '<p class="start-status" role="status">Loading Variant...</p>' : this.#state.errorMessage ? `<p class="start-error" role="alert">${escapeHtml(this.#state.errorMessage)}</p>` : `<div class="configurator-gym-editor-card">${field("Name", `<input class="configurator-gym-input" data-field="name" value="${escapeHtml(this.#name)}" ${this.#isSaving || this.#isDeleting ? "disabled" : ""} />${this.#touched && this.#nameError() ? `<span class="configurator-gym-field-error">${this.#nameError()}</span>` : ""}`)}${v ? `<dl class="configurator-load-profile-metadata"><div><dt>Status</dt><dd>${statusLabel(v.status)}</dd></div></dl>` : ""}${structural}${this.#submitError ? `<p class="start-error" role="alert">${escapeHtml(this.#submitError)}</p>` : ""}<div class="configurator-gym-editor-actions"><button type="button" class="configurator-gym-save-button" data-ui-action="save-configurator-exercise-variant" ${disabled ? "disabled" : ""}>${this.#isSaving ? "Saving..." : v ? "Save Variant" : "Create Variant"}</button>${v?.status === "new" ? `<button type="button" class="configurator-gym-delete-button" data-ui-action="delete-configurator-exercise-variant" ${this.#isDeleting ? "disabled" : ""}>${this.#isDeleting ? "Deleting..." : "Delete Draft"}</button>` : ""}</div></div>`; this.innerHTML = `<div class="app-screen-shell"><button type="button" class="side-menu-toggle detail-back-button" data-ui-action="navigate-back-from-configurator-exercise-variant-detail" aria-label="Back"><span aria-hidden="true">←</span></button><section class="screen-panel configurator-gym-editor-screen" aria-label="Variant editor"><header class="exercise-variant-detail-header configurator-app-header"><img class="start-banner" src="/images/banner.png?v=20260401-2" alt="PumpBuddy banner" /><h1 class="exercise-variant-detail-header-title">${v ? "Variant" : "New Variant"}</h1><p class="exercise-variant-detail-header-subtitle">${escapeHtml(this.#state.exerciseName)}</p></header>${body}</section></div>`; }
}
export const registerPbConfiguratorExerciseVariantEditorScreen = (): void => { if (typeof customElements !== "undefined" && !customElements.get(pbConfiguratorExerciseVariantEditorScreenTag)) customElements.define(pbConfiguratorExerciseVariantEditorScreenTag, PbConfiguratorExerciseVariantEditorScreenElement); };
registerPbConfiguratorExerciseVariantEditorScreen();
