version: 1
kind: backlog_item
item:
  id: pb-046-item-01
  title: Define Workout Detail API Contract
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Add canonical OpenAPI path and response schemas for History workout detail
    retrieval.
  rationale: The renderer and backend need one typed contract source for the new detail
    screen payload.
scope:
  in:
  - Add a workout-detail GET endpoint path and operation metadata to the canonical
    API contract.
  - Define response schema fields for detail hero metadata, completion stats, and
    ordered exercise set lines.
  - Keep naming and payload ownership aligned with existing API contract conventions.
  out:
  - Backend endpoint implementation.
  - Renderer UI wiring and presentation changes.
constraints:
- Do not modify persistence-model or database schema definitions.
- Keep endpoint and payload definitions in agent/design/api-contract.yaml as the source
  of truth.
- Maintain backwards compatibility for existing endpoint schemas.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/design/api-contract.yaml
  optional:
  - agent/design/domain-model.yaml
  - agent/design/workout-use-cases.yaml
acceptance_criteria:
- id: ac-01
  statement: API contract contains one workout-detail retrieval path with complete
    response schema coverage for detail screen needs.
  verification:
    type: command
    command: make check-backend
    expected: Backend quality checks pass with contract updates present.
- id: ac-02
  statement: Contract changes are consumable by renderer/backend typed clients without
    introducing schema collisions.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer quality checks pass after consuming updated contract types.
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - api-contract
  - typed-client-generation
handoff:
  review_focus:
  - Endpoint naming and operation intent consistency.
  - Schema completeness for detail hero, stats, and exercise sections.
review_feedback:
- at: '2026-04-18T14:19:47Z'
  source: review_result
  notes: "findings:\n- criterion: ac-02\n  evidence: 'Deterministic quality gate failed\
    \ during review finalize: agent/scripts/check/verify-api-contract-alignment.sh\n\
    \    exited with ''API contract alignment failed: agent/design/api-contract.yaml\
    \ changed\n    without API surface update'' for source commit fd55145.'\n  risk:\
    \ Contract and implementation can diverge if contract-only changes are merged\n\
    \    without corresponding backend/renderer API surface updates in the same change\n\
    \    set, violating repository alignment policy and risking typed-client mismatch.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'ac-01 met: canonical contract includes one workout-detail retrieval
      path (`GET /api/workouts/{workoutId}/detail`, `getWorkoutDetail`) with `WorkoutDetailResponse`
      coverage for hero metadata, completion stats, and ordered exercise/set lines.
      ac-02 met: follow-up source commit `9ff8750` updates `agent/design/api-contract.yaml`
      and backend API models together, resolving the prior alignment return risk and
      maintaining typed-client consumability.'
    evidence: Verified in `agent/design/api-contract.yaml` under `paths./api/workouts/{workoutId}/detail`
      and `components.schemas.WorkoutDetail*`; prior return condition from contract-only
      source commit `fd55145` is mitigated by paired contract+backend API-surface
      updates in `9ff8750`.
    runtime_build_check: 'Pending finalize: deterministic quality gates run via `agent/scripts/task/review-item/finalize.sh`.'
    residual_risk: Low residual risk remains around future optional-vs-nullable field
      semantics in generated models; current contract and backend mapping are aligned
      within this item scope.
