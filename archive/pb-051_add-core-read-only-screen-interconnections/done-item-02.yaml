version: 1
kind: backlog_item
item:
  id: pb-051-item-02
  title: Preserve expected back-navigation path for progress-origin workout detail
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Workout detail opened from Progress returns users to Progress when navigating
    back.
  rationale: Ensure the new deep-link behavior integrates cleanly with existing navigation
    stack semantics.
scope:
  in:
  - Update navigation stack/state handling for Progress-origin workout detail entry.
  - Preserve browser/app back behavior consistency for the new entry path.
  out:
  - Back behavior changes for History-origin workout detail.
  - Global router redesign or route schema expansion.
constraints:
- Keep interconnection implementation constrained to read-only history/progress pathways.
- Prefer existing navigation stack conventions over custom history handling.
inputs:
  required:
  - agent/execution/plan.yaml
  - agent/execution/workflow-policy.yaml
  optional:
  - agent/design/workout-use-cases.yaml
acceptance_criteria:
- id: ac-01
  statement: After opening workout detail from Progress, invoking back navigation
    returns to Progress instead of an unrelated view.
  verification:
    type: command
    command: make check-renderer
    expected: Command exits successfully with renderer checks passing.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: medium
  boundary_impact:
  - renderer/navigation
  - renderer/workout-detail-screen
handoff:
  review_focus:
  - Validate stack origin metadata is set only for Progress-origin navigation.
  - Verify existing back behavior for other workout-detail entry points is unchanged.
review_feedback:
- at: '2026-04-25T18:52:43Z'
  source: review_result
  notes: "findings:\n- criterion: quality-gate/api-contract-alignment\n  evidence:\
    \ Finalize accept failed in agent/scripts/check/verify-api-contract-alignment.sh\n\
    \    because the implementation commit modifies renderer/src/workout-types.ts\
    \ (API\n    surface file) but does not update agent/design/api-contract.yaml,\
    \ and the gate\n    exits with code 41.\n  risk: If this item is accepted without\
    \ the contract update, documented API contract\n    drifts from implemented interface\
    \ and downstream work may rely on stale schema\n    expectations.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: Progress-origin workout detail now carries returnScreen=progress
      and navigate-history routes back to Progress only when that marker is present;
      history-origin detail remains unmarked and unchanged.
    evidence: Validated controller logic in renderer/src/workout-controller.ts (open-workout-detail
      + navigate-history branches) and regression tests in renderer/src/workout-controller.test.ts
      covering progress-origin and history-origin flows; prior API-contract-alignment
      return finding is resolved by the note added in agent/design/api-contract.yaml.
    runtime_build_check: make check-renderer passed on 2026-04-25 (lint, unit tests,
      coverage, UI smoke, coverage merge all successful).
    residual_risk: 'Low residual risk: behavior depends on viewState.returnScreen
      metadata; future route-shape changes should keep tests for both progress-origin
      and history-origin back paths.'
  findings: []
