version: 1
kind: backlog_item
item:
  id: pb-051-item-08
  title: Converge workout payload vocabulary to canonical contract terms
  status_hint: done
  source:
    type: extended_review
    reference: agent/tmp/review-architecture-findings.yaml
intent:
  outcome: Single canonical field vocabulary is used end-to-end, with legacy aliases
    removed after migration-safe rollout.
  rationale: Improves boundary clarity, reduces adapter complexity, and aligns implementation
    with contract-first expectations.
scope:
  in:
  - Unify backend domain naming with canonical contract semantics for repetition values.
  - Remove renderer fallback aliases that are no longer contract-supported.
  - Align renderer response/request types with canonical schema names.
  out:
  - No new workout feature behavior.
  - No database engine or ORM changes.
constraints:
- Coordinate API and implementation changes together to satisfy api_rules in engineering
  guardrails.
- Provide compatibility migration only where active clients require it, then remove
  dead compatibility paths.
inputs:
  required:
  - agent/design/api-contract.yaml
  - agent/design/domain-model.yaml
  - backend/src/domain.rs
  - renderer/src/workout-types.ts
  - renderer/src/workout-state.ts
  - renderer/src/exercise-performance-derivation.ts
  optional: []
acceptance_criteria:
- id: ac-01
  statement: Workout set and training plan detail payloads use one canonical schema
    vocabulary without duplicate legacy aliases.
  verification:
    type: command
    command: make check
    expected: Backend and renderer checks pass with no reliance on deprecated alias
      fields.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: medium
  boundary_impact:
  - domain model layer
  - api contract and transport models
  - renderer parsing/state adapters
handoff:
  review_focus:
  - Verify API contract and implementation field names change together.
  - Ensure compatibility removal does not break supported flows.
review_feedback:
- at: '2026-04-25T20:14:24Z'
  source: review-architecture:f-02
  notes: The codebase simultaneously carries canonical repetition_value semantics
    and legacy reps/alias fields across domain, renderer payload types, and derivation
    helpers, weakening model-to-contract clarity.
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'Confirmed ac-01: workout-set and training-plan variant payload
      boundaries now expose only canonical repetition vocabulary (repetition_kind/repetition_value)
      and no duplicate legacy alias fields.'
    evidence: 'Implementation commit 0e104cf7 removes legacy alias fields from contract/types/state
      paths: variant_type dropped from TrainingPlanExerciseVariantSummary in agent/design/api-contract.yaml;
      backend domain set structs renamed to repetition_value in backend/src/domain.rs;
      renderer payload types removed optional reps/variant_type aliases in renderer/src/workout-types.ts;
      renderer state mapping removed alias fallbacks set?.reps and selectedOption?.variant_type
      and emits only repetition_value in renderer/src/workout-state.ts. Additional
      boundary scan found no reps or variant_type in backend generated API models
      under backend/src/models and no variant_type in backend API boundary mapping
      in backend/src/api/models.rs.'
    runtime_build_check: Manual full-suite execution was intentionally skipped per
      review policy; acceptance finalize runs deterministic quality gates based on
      implementation-commit changed files.
    residual_risk: 'Low residual risk: internal renderer/UI draft naming reps remains
      intentionally local state terminology, but contract/request/response boundaries
      are canonicalized. Remaining risk is primarily regressions detectable by finalize
      quality gate execution.'
  findings: []
