version: 1
kind: backlog_item
item:
  id: pb-055-item-06
  title: Build renderer station detail and load profile popup
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Add the station detail screen with read-only load profile inspection and
    suitable variant links.
  rationale: Completes the station transparency path once a user drills into a station
    from gym detail or a variant chooser.
scope:
  in:
  - Add station detail renderer state, data loading, and back behavior to the selected
    gym detail.
  - Render station metadata, load profile summary, and grouped/sorted suitable exercise
    variants.
  - Link suitable variants to the existing exercise variant detail route.
  - Add a read-only load profile popup that shows all provided possible values, closes
    explicitly, and avoids selectable affordances.
  out:
  - Gym list screen and gym detail sheet implementation.
  - Backend station detail endpoint implementation.
  - Editing or selecting station load profile values.
constraints:
- Station detail uses a back button and follows existing detail-screen conventions.
- The load profile popup must be explicitly dismissible and must not reuse controls
  that imply mutation or selection.
- Preserve existing exercise variant detail navigation behavior for non-gym-origin
  routes.
inputs:
  required:
  - agent/execution/plan.yaml
  - renderer/src/pb-workout-detail-screen.ts
  - renderer/src/pb-exercise-variant-detail-screen.ts
  - renderer/src/workout-load-display.ts
  - renderer/src/workout-types.ts
  - renderer/src/workout-controller-navigation.ts
  - renderer/src/workout-controller-screen-data.ts
  - renderer/src/styles.scss
  optional:
  - renderer/src/pb-workout-detail-screen.test.ts
  - renderer/src/pb-exercise-variant-detail-screen.test.ts
  - renderer/src/workout-load-display.test.ts
  - renderer/src/workout-controller.test.ts
acceptance_criteria:
- id: ac-01
  statement: Station detail renders station metadata, load profile summary, and grouped/sorted
    suitable variants with links to exercise variant detail.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer checks pass with station detail rendering and variant-link
      coverage.
- id: ac-02
  statement: The load profile popup presents possible values read-only, closes only
    through explicit dismissal, and does not expose selectable or editable controls.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer checks pass with popup open/close and read-only affordance
      coverage.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: medium
  boundary_impact:
  - renderer-navigation
  - renderer-state
  - renderer-ui
  - load-profile-display
handoff:
  review_focus:
  - The load profile popup reads as inspection-only and cannot change application
    data.
  - Back behavior returns to the correct gym detail context after station detail and
    variant detail navigation.
review_feedback:
- at: '2026-05-29T20:59:57Z'
  source: review_result
  notes: "findings:\n- criterion: api_contract_alignment\n  evidence: 'Accept finalization\
    \ for implementation commit 21d2d078 failed in agent/scripts/check/verify-api-contract-alignment.sh\n\
    \    with: API surface changed without updating agent/design/api-contract.yaml.\
    \ The\n    source commit changed renderer/src/workout-api.ts and did not include\
    \ agent/design/api-contract.yaml.'\n  risk: The item cannot be accepted because\
    \ the deterministic review quality gate\n    fails. A corrective implementation\
    \ pass must include canonical API contract alignment\n    evidence/update alongside\
    \ the renderer API-surface change.\n  requires_api_contract_update: true\n"
  findings:
  - criterion: api_contract_alignment
    evidence: 'Accept finalization for implementation commit 21d2d078 failed in agent/scripts/check/verify-api-contract-alignment.sh
      with: API surface changed without updating agent/design/api-contract.yaml. The
      source commit changed renderer/src/workout-api.ts and did not include agent/design/api-contract.yaml.'
    risk: The item cannot be accepted because the deterministic review quality gate
      fails. A corrective implementation pass must include canonical API contract
      alignment evidence/update alongside the renderer API-surface change.
    requires_api_contract_update: true
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'AC-01 and AC-02 are met: station detail renders station metadata,
      load profile summary, grouped suitable variants, and exercise-variant links;
      the load-profile popup presents static possible values with explicit dismissal
      and no editable/selectable controls.'
    evidence: Renderer station detail and popup implementation/test coverage landed
      in commits 21d2d078 and 12c459fa; corrective API contract alignment evidence
      landed in 8d2ff5f5 for GET /api/gyms/{gymId}/stations/{stationId} and GymStationDetailResponse
      consumption.
    runtime_build_check: 'make check-renderer passed on 2026-05-30: 28 test files
      and 391 tests passed in unit and coverage runs, production build succeeded,
      and UI smoke passed in chromium, firefox, and webkit. Accept finalization will
      run the API contract-alignment gate for 8d2ff5f5.'
    residual_risk: Low. The current review source commit focuses on API alignment,
      so renderer verification is captured by the explicit make check-renderer run;
      no remaining blocking findings are known.
  findings: []
