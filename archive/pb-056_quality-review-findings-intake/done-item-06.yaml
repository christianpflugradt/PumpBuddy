version: 1
kind: backlog_item
item:
  id: pb-056-item-06
  title: Route backend use cases through application services
  status_hint: done
  source:
    type: extended_review
    reference: agent/tmp/review-architecture-findings.yaml
intent:
  outcome: API handlers delegate workout, training-plan, and gym use cases to application-layer
    service functions while handlers stay focused on HTTP extraction and response
    mapping.
  rationale: A stable application boundary keeps transport concerns from coupling
    directly to persistence and gives business rules a single place to live.
scope:
  in:
  - Introduce or extend application service functions for read and command use cases
    currently handled directly in API handlers.
  - Move visibility checks, use-case orchestration, and persistence error interpretation
    out of handlers where practical.
  - Keep OpenAPI route paths and payload shapes stable unless an existing contract
    mismatch is discovered.
  out:
  - Rewriting sqlx persistence modules.
  - Changing renderer navigation or UI state.
  - Introducing a dependency injection framework.
constraints:
- Preserve Axum as the HTTP transport.
- Keep repository traits narrow and use-case oriented.
- Use make targets for verification.
inputs:
  required:
  - agent/strategy/engineering-guardrails.yaml
  - backend/src/api
  - backend/src/application
  - backend/src/persistence
  optional:
  - agent/design/workout-use-cases.yaml
acceptance_criteria:
- id: ac-01
  statement: Workout, training-plan, and gym handlers no longer call concrete DomainRepository
    methods for use-case orchestration.
  verification:
    type: command
    command: make check-backend
    expected: Backend quality passes with handlers delegating through application
      services.
- id: ac-02
  statement: Existing HTTP contract behavior is preserved for current endpoints.
  verification:
    type: command
    command: make check-backend
    expected: Backend API and integration tests pass without OpenAPI endpoint shape
      changes.
execution:
  plan_item_required: true
  plan_item_skip_reason: null
  risk_level: medium
  boundary_impact:
  - backend api
  - backend application
  - backend persistence
handoff:
  review_focus:
  - Check that handlers only map HTTP inputs/outputs and do not recreate use-case
    decisions.
  - Check that application services expose user-scoped operations rather than raw
    persistence reads.
  - Check that error mapping remains predictable at the API boundary.
review_feedback:
- at: '2026-05-30T09:19:36Z'
  source: review-architecture:f-02
  notes: The transport layer owns the concrete DomainRepository and many handlers
    call persistence methods directly. Application logic exists for selected auth/workout
    validation, but read use cases and several command paths are orchestrated in handlers
    instead of an application service boundary.
- at: '2026-05-30T11:32:31Z'
  source: review_result
  notes: "findings:\n- criterion: ac-02 API contract alignment\n  evidence: 'Accept\
    \ finalization failed in agent/scripts/check/verify-api-contract-alignment.sh\n\
    \    with exit 41: implementation commit c79f9eea changed backend/src/api/handlers/gyms.rs,\n\
    \    backend/src/api/handlers/training_plans.rs, and backend/src/api/handlers/workouts.rs\n\
    \    without an agent/design/api-contract.yaml delta. Code review did not identify\n\
    \    route or payload shape drift, but the repository quality gate treats backend/src/api/**\n\
    \    edits as API surface changes requiring canonical contract evidence.'\n  risk:\
    \ The review item cannot be accepted or released through the deterministic quality\n\
    \    gate; future API-surface changes may be indistinguishable from internal handler\n\
    \    refactors unless the corrective pass includes contract alignment evidence.\n\
    \  requires_api_contract_update: true\n"
  findings:
  - criterion: ac-02 API contract alignment
    evidence: 'Accept finalization failed in agent/scripts/check/verify-api-contract-alignment.sh
      with exit 41: implementation commit c79f9eea changed backend/src/api/handlers/gyms.rs,
      backend/src/api/handlers/training_plans.rs, and backend/src/api/handlers/workouts.rs
      without an agent/design/api-contract.yaml delta. Code review did not identify
      route or payload shape drift, but the repository quality gate treats backend/src/api/**
      edits as API surface changes requiring canonical contract evidence.'
    risk: The review item cannot be accepted or released through the deterministic
      quality gate; future API-surface changes may be indistinguishable from internal
      handler refactors unless the corrective pass includes contract alignment evidence.
    requires_api_contract_update: true
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'ac-01 and ac-02 are met: workout, training-plan, and gym handlers
      delegate use-case orchestration to application services, and the corrective
      API contract note records that endpoint paths and payload schemas are unchanged.'
    evidence: Search evidence shows the target handlers call application service functions
      with &state.repository rather than concrete DomainRepository methods for orchestration.
      Service modules backend/src/application/workouts.rs, backend/src/application/training_plans.rs,
      and backend/src/application/gyms.rs own validation sequencing, visibility/not-found
      decisions, and persistence delegation. agent/design/api-contract.yaml now includes
      the 2026-05-30 backend application-service routing note confirming no endpoint
      or payload-schema changes.
    runtime_build_check: The corrective implementation ran make check-backend successfully
      after the API contract note was added; accept finalization will run the deterministic
      review quality gate for the current source commit.
    residual_risk: Residual risk is low. WorkoutRepository remains broader than the
      gym/training-plan service traits, but it is bounded to workout use cases, and
      existing backend API/integration coverage exercises the preserved HTTP contract.
  findings: []
