version: 1
kind: backlog_item
item:
  id: pb-040-item-02
  title: Rename backend workout selection field usage
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Backend domain, persistence, and workout application layers use `selected_training_plan_exercise_variant_id`
    terminology consistently.
  rationale: Aligns backend runtime logic with renamed persistence identifiers and
    removes ambiguous legacy naming.
scope:
  in:
  - Update backend models and mapping code from `selected_plan_exercise_option_id`
    to `selected_training_plan_exercise_variant_id`.
  - Update backend training-plan/workout persistence query aliases and method naming
    that expose plan option terminology.
  out:
  - OpenAPI schema and generated API client model regeneration.
  - Renderer-side field renames.
constraints:
- Preserve existing validation semantics and option-to-variant/station matching behavior.
- Keep backend internal-only topology and trust boundaries unchanged.
inputs:
  required:
  - agent/execution/plan.yaml
  - backend/src/domain.rs
  - backend/src/persistence
  - backend/src/application/workouts.rs
  optional:
  - backend/src/api/models.rs
acceptance_criteria:
- id: ac-01
  statement: Active backend code paths no longer rely on `selected_plan_exercise_option_id`
    as the primary field name.
  verification:
    type: command
    command: rg -n "selected_plan_exercise_option_id" backend/src
    expected: No active backend source references remain except intentional compatibility
      shims (if any).
- id: ac-02
  statement: Backend quality checks validate renamed field wiring end-to-end.
  verification:
    type: command
    command: make check-backend
    expected: Backend quality/test suite passes.
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - backend
handoff:
  review_focus:
  - Field rename consistency across domain models, persistence mapping, and validation
    messages.
review_feedback:
- at: '2026-04-12T16:39:02Z'
  source: review_result
  notes: "findings:\n- criterion: ac-02\n  evidence: 'Running agent/scripts/task/review-item/finalize.sh\
    \ 02 accept executed\n    make check-backend successfully but then failed QUALITY_GATE_RUN=agent/scripts/check/verify-api-contract-alignment.sh\n\
    \    with ''API contract alignment failed: API surface changed without updating\
    \ agent/design/api-contract.yaml''.'\n  risk: Accepting despite this gate failure\
    \ would allow backend API/model surface\n    drift relative to the canonical contract\
    \ and break deterministic quality policy\n    for this item.\n"
- at: '2026-04-12T16:56:41Z'
  source: review_result
  notes: "findings:\n- criterion: scope.in[2]\n  evidence: Backend training-plan/workout\
    \ persistence naming still exposes legacy\n    plan option terminology in active\
    \ method signatures and call sites, including\n    `fetch_plan_exercise_option_summaries*`\
    \ in `backend/src/application/workouts.rs`\n    (lines 162, 266, 321), `backend/src/persistence/mod.rs`\
    \ (lines 79, 94), and `backend/src/persistence/training_plans.rs`\n    (lines\
    \ 432, 510).\n  risk: Leaving legacy plan-option naming in active backend persistence/application\n\
    \    interfaces violates this item's explicit scope and preserves ambiguous terminology\n\
    \    that can cause future mapping mistakes during ongoing variant rename work.\n"
- at: '2026-04-12T18:40:06Z'
  source: review_result
  notes: "findings:\n- criterion: ac-02\n  evidence: 'Running `agent/scripts/task/review-item/finalize.sh\
    \ 02 accept` executed\n    `make check-backend` successfully but failed `QUALITY_GATE_RUN=agent/scripts/check/verify-api-contract-alignment.sh`\n\
    \    with `API contract alignment failed: API surface changed without updating\
    \ agent/design/api-contract.yaml`.\n    The gate reported backend/API-facing changes\
    \ in `backend/src/api/handlers/training_plans.rs`,\n    `backend/src/application/workouts.rs`,\
    \ `backend/src/persistence/mod.rs`, `backend/src/persistence/training_plans.rs`,\n\
    \    and related backend integration-test files.'\n  risk: Accepting despite this\
    \ deterministic gate failure would allow backend/API\n    behavior to drift from\
    \ `agent/design/api-contract.yaml`, violating review acceptance\n    policy and\
    \ increasing the chance of contract/runtime mismatch across backend and\n    renderer.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: ac-01 and ac-02 satisfied for this item scope; backend naming is
      consistently aligned to `selected_training_plan_exercise_variant_id` and legacy
      method naming drift called out in prior returns is resolved.
    evidence: Implementation commit 7f54e37 keeps backend runtime field usage on `selected_training_plan_exercise_variant_id`,
      renames API-layer training-plan "options" symbols to variant terminology in
      `backend/src/api/models.rs`, `backend/src/api/handlers/training_plans.rs`, `backend/src/api/handlers/mod.rs`,
      and `backend/src/api/router.rs`, and updates `agent/design/api-contract.yaml`
      operationId to keep deterministic API-contract alignment checks in sync.
    runtime_build_check: rg -n "selected_plan_exercise_option_id" backend/src returned
      no active source matches during review, and accept finalize runs deterministic
      quality gates via agent/scripts/check/run-quality-gate.sh against implementation
      commit 7f54e37.
    residual_risk: Transitional external API naming still uses the `/options` route
      path for compatibility while backend internals and contract operation naming
      now consistently use variant terminology; risk is documentation/discoverability
      only and does not affect runtime behavior.
