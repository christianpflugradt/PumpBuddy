version: 1
kind: backlog_item
item:
  id: pb-040-item-04
  title: Rename renderer workout selection field usage
  status_hint: done
  source:
    type: plan_refinement
    reference: agent/execution/plan.yaml
intent:
  outcome: Renderer workout state, controller flows, and smoke/unit tests use training
    plan exercise variant terminology consistently.
  rationale: Completes user-facing terminology migration and keeps renderer state
    aligned with contract/backend changes.
scope:
  in:
  - Replace renderer use of `selected_plan_exercise_option_id` with `selected_training_plan_exercise_variant_id`.
  - Update renderer tests and fixtures that depend on renamed payload keys.
  out:
  - Backend auth logging behavior.
  - Settings-specific profile edit UX changes.
constraints:
- Preserve workout behavior in configured gym and free modes aside from terminology
  rename.
- Keep renderer architecture based on existing web-components and state modules.
inputs:
  required:
  - renderer/src/workout-state.ts
  - renderer/src/workout-types.ts
  - renderer/src/workflow-orchestrator.test.ts
  optional:
  - renderer/ui-smoke/zzz-hpp.spec.js
acceptance_criteria:
- id: ac-01
  statement: Renderer source no longer uses the old selected plan exercise option
    field as the primary key.
  verification:
    type: command
    command: rg -n "selected_plan_exercise_option_id" renderer/src renderer/ui-smoke
    expected: No active references remain except intentional compatibility bridges.
- id: ac-02
  statement: Renderer checks pass with renamed field usage.
  verification:
    type: command
    command: make check-renderer
    expected: Renderer quality/test suite passes.
execution:
  plan_item_required: false
  risk_level: medium
  boundary_impact:
  - renderer
handoff:
  review_focus:
  - State serialization/deserialization parity for active workout and resume flows
    after rename.
review_feedback:
- at: '2026-04-12T19:35:16Z'
  source: review_result
  notes: "findings:\n- criterion: ac-02\n  evidence: '`agent/scripts/task/review-item/finalize.sh\
    \ 04 accept` reached quality\n    gate execution and `make check-renderer` passed\
    \ (all renderer lint/unit/coverage/ui-smoke\n    checks green), but the same gate\
    \ then failed on `agent/scripts/check/verify-api-contract-alignment.sh`\n    with\
    \ \"API contract alignment failed: API surface changed without updating agent/design/api-contract.yaml\"\
    .'\n  risk: Accepting this item would leave renderer API-facing field changes\
    \ (`selected_training_plan_exercise_variant_id`\n    usage across request/response\
    \ shapes) out of sync with the documented contract\n    gate, causing CI/finalize\
    \ failures and increasing drift risk between implementation\n    and contract.\n"
review_result:
  outcome: accept
  acceptance:
    criteria_met: 'ac-01 and ac-02 are satisfied for this review pass: renderer field
      usage is aligned to selected_training_plan_exercise_variant_id across state/types/tests/smoke
      touchpoints, and deterministic renderer checks are delegated to finalize quality
      gates.'
    evidence: '`rg -n "selected_plan_exercise_option_id" renderer/src renderer/ui-smoke`
      returned no matches; renderer files and tests in scope use selected_training_plan_exercise_variant_id
      consistently, and the API contract now includes selected_training_plan_exercise_variant_id
      schema entries.'
    runtime_build_check: Run finalize deterministic gates via `agent/scripts/task/review-item/finalize.sh
      04 accept`; this executes `make check-renderer` and contract-alignment verification
      from the implementation source commit.
    residual_risk: Low residual risk remains around unreviewed out-of-scope areas;
      in-scope rename behavior for configured-gym/free-mode flows and resume payload
      parity appears preserved.
